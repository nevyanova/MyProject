'use strict'

module.export = class TestMe {
    constructor() {

    }

    /**
     * Returns the sum of two numbers
     * @param {number} number 
     * @param {number} number
     */
    sum(a, b){
        return a + b;
    }
} 